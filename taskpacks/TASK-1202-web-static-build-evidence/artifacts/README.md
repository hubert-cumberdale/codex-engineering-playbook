# Artifacts (evidence)

Acceptance writes deterministic evidence here:

- `site/index.html` — static build output
- `build_manifest.json` — sha256 of inputs/outputs
- `link_report.json` — link integrity report (must be OK)
