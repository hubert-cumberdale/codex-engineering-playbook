# Artifacts (evidence)

Acceptance writes evidence here:

- `validation_report.json` — validation status + broken link list (must be empty)
- `content_bundle.zip` — deterministic bundle of `content/` for downstream use

This Task Pack is a content pipeline example only (no engine build, no deployment).
